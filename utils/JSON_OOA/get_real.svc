public service JSON::get_real(json_element: in JSONElement) return real is
begin
if json_element.kind = Real then
  return json_element.data.real;
else
  raise JSONException("JSON element is not a real");
end if;
end service;

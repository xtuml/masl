public service JSON::to_string1(obj: in JSONObject) return string is
jsonFields: sequence of string;
begin
  for k in obj'keys loop
    jsonFields := jsonFields & ("\"" & k & "\"" & ":" & to_string2(obj[k]));
  end loop;
  return "{" & Strings::join(jsonFields, ",") & "}";
end service;
